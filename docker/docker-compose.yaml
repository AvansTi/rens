services:   
  base:
    image: rens-${DEPLOY}:${USE_ROS_DISTRO}
    build:
      context: ../
      dockerfile: docker/Dockerfile
      args:
        - RUNTIME=${RUNTIME}
        - DEPLOY=${DEPLOY}
        - UBUNTU_VER=${UBUNTU_VER}
        - USE_ROS_DISTRO=${USE_ROS_DISTRO}
        - ROBOT_BASE=${ROBOT_BASE}
        - LASER_SENSOR=${LASER_SENSOR} 
        - DEPTH_SENSOR=${DEPTH_SENSOR}
        - RMW=${RMW}
      target: base

  dev:
    runtime: ${RUNTIME}
    image: rens-${DEPLOY}:${USE_ROS_DISTRO}
    hostname: rens-${USE_ROS_DISTRO}
    stdin_open: true
    tty: true
    network_mode: host
    ipc: host
    privileged: true
    environment:
      - ROS_DOMAIN_ID=${ROS_DOMAIN_ID}
      - RENS_BASE=${ROBOT_BASE} 
      - RENS_LASER_SENSOR=${LASER_SENSOR} 
      - RENS_DEPTH_SENSOR=${DEPTH_SENSOR} 
      - DISPLAY=${DISPLAY} 
      - QT_X11_NO_MITSHM=1
      - WORLD=${WORLD:-playground}
    volumes:
      - /tmp/.X11-unix:/tmp/.X11-unix:rw
      - $HOME/.Xauthority:/root/.Xauthority
      - ../rens_navigation/maps:/root/rens_ws/src/rens/rens_navigation/maps:rw)

  gazebo:
    container_name: gazebo
    extends: dev
    command: > 
      ros2 launch rens_gazebo gazebo.launch.py 
      world:=/root/rens_ws/install/rens_gazebo/share/rens_gazebo/worlds/${WORLD:-turtlebot3_world}.sdf
      spawn_x:=${SPAWN_X:-0.5}
      spawn_y:=${SPAWN_Y:-0.0}
      spawn_yaw:=${SPAWN_Z:-0.0}
    devices:
      - /dev:/dev

  bringup:
    container_name: bringup
    extends: dev
    command: >
      ros2 launch rens_bringup bringup.launch.py 
      custom_robot:=${CUSTOM_ROBOT}
      extra:=${LAUNCH_EXTRA} 
      odom_topic:=${ODOM_TOPIC}
      base_serial_port:=${BASE_SERIAL_PORT}
      joy:=${LAUNCH_JOYSTICK}
    devices:
      - /dev:/dev

  slam:
    container_name: slam
    extends: dev
    command: >
      ros2 launch rens_navigation slam.launch.py
      rviz:=${RVIZ:-false}
      sim:=${SIM:-false}

  navigate:
    container_name: navigate
    extends: dev
    command: >
      ros2 launch rens_navigation navigation.launch.py
      map:=/root/rens_ws/src/rens/rens_navigation/maps/${WORLD:-turtlebot3_world}.yaml
      rviz:=${RVIZ:-false}
      sim:=${SIM:-false}

  save-map:
    container_name: save-map
    extends: dev
    command: >
      ros2 run nav2_map_server map_saver_cli 
      -f /root/rens_ws/src/rens/rens_navigation/maps/map
      --fmt png
      --ros-args -p save_map_timeout:=10000.

  rviz-nav:
    container_name: rviz-nav
    extends: dev
    command: rviz2 -d /root/rens_ws/src/rens/rens_navigation/rviz/rens_navigation.rviz

  rviz:
    container_name: rviz
    extends: dev
    command: rviz2

  debug:
    container_name: debug
    extends: dev
    command: sleep infinity
    volumes:
    - ../.:/root/rens_ws/src/rens

  webtop:
    container_name: webtop
    image: lscr.io/linuxserver/webtop:latest
    environment:
    - DISPLAY=:200
    network_mode: host
    devices:
    - /dev/dri:/dev/dri 

  kasmvnc:
    container_name: kasmvnc
    image: lsiobase/kasmvnc:alpine321
    environment:
    - DISPLAY=:200
    network_mode: host
    devices:
    - /dev/dri:/dev/dri
